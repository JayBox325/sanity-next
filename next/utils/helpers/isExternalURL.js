const isExternalURL = (url) => {
    console.log(`Checking if '${url}' is an external url`)

    /*
        1. Need to check if it is a valid URL
        2. Compare it to the window domain
        3. If doesn't match, return true
    */ 

    return false
}


export default isExternalURL